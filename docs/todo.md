# TODO

- [x] 업무모듈 목록에 `임시저장` 토글 필터 공통 적용
  - 대상: PM, WO, WP, PR
  - 활성화 시 `status = 'T'`이고 작성자가 현재 사용자인 문서만 조회
  - 비활성화 시 `status <> 'T'`인 일반 문서 목록 조회
  - 임시저장 문서는 본인만 조회·수정·삭제
  - 별도 장바구니 기능은 만들지 않고, 구매요청 화면에서 본인의 PR 임시저장 목록으로 대체

- [x] 권한 및 반려문서 정책 적용
  - 모든 외부 API는 `PermissionGuard`에서 사용자 Role의 `module + CRUD`를 검사
    - `GET` → R, `POST` → C, `PUT/PATCH` → U, `DELETE` → D
  - `WorkflowPermission`, `RefPermission`과 이중 권한 레벨 구조는 제거
    - Ref API도 해당 모듈의 R 권한으로 처리
    - 상태 전이 API는 명시된 모듈 CRUD 권한으로 진입 후 Service에서 업무조건 확인
  - Admin은 Guard에서 무조건 우회하지 않고 전체 CRUD가 부여된 ADMIN Role로 처리
  - System 내부 API만 별도 SystemGuard로 분리
  - MDM/Master는 모듈별 C/R/U/D 권한으로 관리
  - PM/WO/WP/PR/PO도 U/D 권한을 먼저 확인한 후 Service에서 문서 상태·소유자를 확인
  - 업무문서의 임시저장(T)은 U/D 권한과 작성자 본인 여부를 모두 확인하여 수정·삭제
  - P/C/S 상태의 업무문서는 일반 수정·삭제 불가
  - 반려문서(R)의 수정은 기존 문서 변경이 아니라 데이터를 복사한 신규 문서 작성으로 처리
  - 기존 반려문서와 APR 이력은 보존하고 신규 문서는 새 결재문서로 상신
  - 재고문서는 임시저장 없이 module + CRUD 권한 적용
  - 사용자의 Role은 필수값으로 저장
    - 사용자 생성 시 부서 기본 Role을 초기값으로 복사
    - 이후에는 사용자 Role을 기준으로 권한을 검사하며 자동 상속하지 않음
    - 사용자 관리 권한자만 개별 Role 변경 가능
  - Scope는 모듈별로 나누지 않고 회사 전체 공통값으로 관리
    - 부서 Scope와 사용자 Scope 모두 필수값이며 NULL을 사용하지 않음
    - 사용자 생성 시 부서 Scope를 기본값으로 복사
    - 이후에는 사용자 Scope만 사용하며 자동 상속하지 않음
    - `COMPANY`는 Plant 비교 없이 회사 범위만 확인
    - `PLANT`는 문서 Plant와 현재 `activePlantId`가 일치하는지 확인
    - 목록은 Query 조건으로, 상세·생성·수정·삭제는 Service에서 Plant 범위 재확인
  - 임시문서·Scope·결재선·첨부파일 정책은 `PermissionPolicyService`에 공통화
    - 임시문서: T 상태 + 본인 여부
    - 게시판: BRD U/D 권한 + 작성자 본인 여부
    - 첨부파일: 임시 첨부 본인 여부 또는 연결 원문서의 상태·소유자·모듈 확인
    - 결재: 기안자·결재선 참여자·현재 처리 순서·문서 상태 확인
  - `PermissionPolicyService`는 모듈 CRUD와 문서 업무조건을 분리
  - [x] 별도 Ref API 및 RefPermission을 제거하고 기존 GET API와 제한 DTO를 사용

- [x] 출력폼 표준화
  - 공통 Header: 회사코드, 출력일시, 출력자, 우측 페이지 `n / total`
  - Footer는 사용하지 않음
  - 목록 출력은 문서 Header 정보 중심으로 구성하고 상세 아이템은 제외
  - 개별 출력은 공통 기본정보와 문서별 상세 항목을 구분
  - 공통 기본정보: 문서번호, 작성일자, 부서명, 작성자, 제목, 대상설비, 유형
  - 제목은 점검명·지시명·허가명 등을 통합해 사용
  - PM은 계획서·결과보고서로 구분
  - WO도 계획서·실적보고서로 구분
  - WP·PR·PO는 계획·요청·발주 Header와 개별 상세 항목을 분리
  - 재고 전표는 입고·출고·이동·조정 유형을 Header에 표시
  - 재고 전표의 상세 아이템은 자재·수량·단위로 구성
  - 현재는 별도 출력 도메인 모듈보다 공통 출력 컴포넌트 확장이 우선
  - 개별 출력 항목
    - PM 계획: 계획일·계획기간, 비고, 점검항목·점검방법·기준범위·단위
    - PM 실적: 점검일·판정·인증정보, 비고, 점검항목·점검방법·기준범위·측정값·단위
    - WO 계획: 계획일, 비고, 작업내용·작업방법·예정 공수·비용
    - WO 실적: 작업일, 비고, 작업내용·작업방법·작업결과·실제 공수·비용
    - WP: 계획일·허가기간·감독자·연계 작업지시, 비고, 위험요인·안전대책·LOTO 체크시트
    - PR: 요청일·사업장·요청창고·요청유형, 비고, 요청 자재·수량·단위
    - PO: 발주일·납품창고·예정도착일·배송시작일·구매담당자, 비고, 발주 자재·수량·단위·품목 비고
    - 재고 전표: 전표 유형(입고·출고·이동·조정)·처리창고, 자재·수량·단위
    - 결재 품의서: 결재번호·기안부서·기안자·제목·본문·첨부파일·결재의견
  - 목록폼과 개별폼의 공통 항목명 통일
    - 계획번호·실적번호 → 문서번호
    - 점검명·지시명·허가명 → 제목
    - 설비명 → 대상설비
    - 점검유형·작업유형 → 유형
    - 목록과 개별폼 모두 문서번호·작성일자·부서명·작성자·제목·대상설비·유형 사용
